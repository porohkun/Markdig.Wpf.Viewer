<Window x:Class="MarkdigWpfViewer.Demo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MarkdigWpfViewer.Demo"
        xmlns:markdigWpfViewer="clr-namespace:MarkdigWpfViewer;assembly=Markdig.Wpf.Viewer"
        mc:Ignorable="d"
        Title="MainWindow"
        d:DataContext="{d:DesignInstance local:MainWindowViewModel, IsDesignTimeCreatable=True}"
        Height="800" Width="600">
    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>
    <ScrollViewer>
        <markdigWpfViewer:MarkdownViewer Markdown="{Binding Text}" Margin="8"/>
    </ScrollViewer>
</Window>
